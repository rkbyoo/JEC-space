import React from 'react'

function ProfileInfo() {
  return (
    <div>
      the component to render the profile info from global state where user id is saved after login
    </div>
  )
}

export default ProfileInfo
